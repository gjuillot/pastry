<%= form_for(@price) do |f| %>
  <% if @price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price.errors.count, "error") %> prohibited this price from being saved:</h2>

      <ul>
      <% @price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ingredient %><br />
    <%= f.text_field :ingredient, value: @ingredient.name, disabled: true %>
    <%= f.hidden_field :ingredient_id, value: @ingredient.id %>
  </div>
  <div class="field">
    <%= f.label :place %><br />
    <%= f.text_field :place %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %><br />
    <%= @ingredient.unit %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %> €
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
