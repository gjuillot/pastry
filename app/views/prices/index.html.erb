<h1>Listing prices</h1>

<table>
  <tr>
    <th>Ingredient</th>
    <th>Price</th>
    <th>Place</th>
    <th>Date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @prices.each do |price| %>
  <tr>
    <td><%= price.ingredient.name %></td>
    <td><%= "#{price.price} € pour #{price.quantity} #{price.ingredient.unit}"%></td>
    <td><%= price.place %></td>
    <td><%= price.created_at %></td>
    <td><%= link_to 'Show', price %></td>
    <td><%= link_to 'Edit', edit_price_path(price) %></td>
    <td><%= link_to 'Destroy', price, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Price', new_price_path %>
